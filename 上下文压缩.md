# 上下文压缩.md（2026-03-16）

## 1. 项目目标（压缩版）
1. 面向刀具磨损预测，建立可复现的跨工况实验流程。
2. 重点解决：数据加载策略、特征策略、跨工况泛化。
3. 当前实验主设置：`c1,c4 -> c6`，走刀级（pass-level）。

## 2. 已完成里程碑
1. 阶段A：入口参数统一与数据透传完成（run/data_factory/data_loader）。
2. 阶段B：双 DataLoader 混合训练完成（c1/c4 分别构窗，batch 级 shuffle 混采）。
3. 阶段C（旧）：133特征 quick 与长训跑通；DP 场景测试指标缺失已修复并补测。
4. 标准化一致性：已统一为训练域共享 scaler；测试反归一化链路已验证。
5. 公平性：Dual/Single 训练步数已对齐。

## 3. 当前策略变更（用户最新确认）
1. 暂停“冗余消除”路线（跨域泛化风险高）。
2. 暂停“133特征继续扩展”路线。
3. 启动“原始数据重提取 + 树模型特征选择”路线。
4. 特征选择必须严格只在训练域执行（不使用验证/测试信息）。

## 4. 新增独立实现（不依赖旧 feature_selection 代码）
目录：`feature_extraction/`
1. `pipeline_tree_selection.py`
2. `run_tree_pipeline.sh`
3. `README.md`

实现特性：
1. 从 `dataset/c1,c4,c6` 原始 csv 直接提取走刀级时域特征。
2. 当前首轮特征集：`td28`（7通道 × 4统计量）。
3. 树模型：`RandomForestRegressor`，多 seed 平均重要性。
4. 输出：`keep_features`、重要性 csv、筛选后可训练 npz。

## 5. 阶段C首轮执行结果（已完成）
1. 命令：`bash feature_extraction/run_tree_pipeline.sh`
2. 训练矩阵：`X(548,28), y(548,)`（c1/c4 训练覆盖切片）
3. 产物目录：
   - `dataset/passlevel_tree_select/base_td28/`
   - `dataset/passlevel_tree_select/selected_td28_k16/`
4. Top-16：
   - `Feat_20, Feat_8, Feat_6, Feat_24, Feat_7, Feat_18, Feat_19, Feat_2, Feat_10, Feat_4, Feat_23, Feat_12, Feat_1, Feat_22, Feat_11, Feat_25`

## 6. 下一步（待执行）
1. 用 `selected_td28_k16` 接入现有 dual-loader 训练协议。
2. 先跑 `c1,c4 -> c6` 指标（window/fullcurve/fullcurve_raw）。
3. 再扩展到三折交叉工况（`c1,c6->c4`、`c4,c6->c1`）。

## 7. 关键文件清单
1. 过程与计划：
   - `process.md`
   - `实施计划.md`
   - `记录我们两个问答的记录.md`
   - `结果.md`
2. 新特征流程：
   - `feature_extraction/pipeline_tree_selection.py`
   - `feature_extraction/run_tree_pipeline.sh`
   - `feature_extraction/README.md`
