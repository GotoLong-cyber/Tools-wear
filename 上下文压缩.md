# 上下文压缩.md（2026-03-24）

## 1. 项目目标（压缩版）
1. 面向刀具磨损预测，建立可复现的跨工况实验流程。
2. 重点解决：数据加载策略、特征策略、跨工况泛化。
3. 当前实验主设置：`c1,c4 -> c6`，走刀级（pass-level）。

## 2. 已完成里程碑
1. 阶段A：入口参数统一与数据透传完成（run/data_factory/data_loader）。
2. 阶段B：双 DataLoader 混合训练完成（c1/c4 分别构窗，batch 级 shuffle 混采）。
3. 阶段C（旧）：133特征 quick 与长训跑通；DP 场景测试指标缺失已修复并补测。
4. 标准化一致性：已统一为训练域共享 scaler；测试反归一化链路已验证。
5. 公平性：Dual/Single 训练步数已对齐。

## 3. 当前策略变更（最新版）
1. 树模型全局 top-k 路线已验证过，但跨工况泛化不理想，不再作为当前主线。
2. 当前稳定特征基线是 `RMS7 + Feat_4`（共 8 维）。
3. 当前主要矛盾已从“继续找更多特征”切换为“先解决时间尺度错位”。
4. 所有新实验必须满足：
   - GPU 强约束；
   - 严格零泄露；
   - 三折交叉工况评估；
   - 主指标优先看 `fullcurve_raw MAE/RMSE`。
5. 当前正式训练协议补充：
   - 采用 `partial backbone tuning`；
   - 冻结大部分 backbone；
   - 保留全部 block 的 `out_projection` 可训练；
   - 解冻最后 `1` 个 block；
   - 再训练 head；
   - 当前可训练参数比例约 `23.42%`，这是有意设计，不视为 bug。

## 4. 当前主结果（最新）
1. A1 已完成：
   - 已证据化证明 `c6` 更早进入高磨损区，存在明显时间尺度错位。
2. 旧版 A2 `stagealign`：
   - 数值上在部分折有收益；
   - 但因 train/test 轴语义不一致、future-informed preprocessing 风险，已降级为诊断实验，不再作为正式方法。
3. 新版 A2 `dynstride`：
   - 含义：训练时动态步长增强；
   - 本质：在原始时间域内，只对训练集低斜率窗口允许更大步长取窗；
   - `val/test` 固定 `stride=1`；
   - 不使用测试标签，不对测试序列做重采样。
4. 新版 A2 三折 `fullcurve_raw MAE`：
   - `c1,c4 -> c6`: `13.6211 -> 10.4528`
   - `c4,c6 -> c1`: `7.0051 -> 4.0110`
   - `c1,c6 -> c4`: `5.2484 -> 4.3283`
   - 三折平均：`8.6249 -> 6.2640`（约 `-27.37%`）
5. 当前 hardest fold 仍为 `c1,c4 -> c6`。

## 5. 当前新增诊断与可视化
1. 已生成新版 A2 动态步长诊断图：
   - `feature_alignment_diagnosis/outputs/A2_dynstride_diagnostics_20260323_1743/`
2. 产物包含：
   - 原始/单调磨损曲线；
   - 归一化磨损进度曲线；
   - 三折局部斜率阈值图；
   - 三折可增强窗口分布图；
   - 三折斜率直方图；
   - 图解 `md/html`。

## 6. 下一步（已规划，未执行）
目标：先补机制消融，说明 `A2_dynstride` 为什么有效，而不是直接继续加特征。

三组优先消融：
1. `随机 stride` vs `斜率引导 stride`
   - 证明不是“任何 stride 增强都有效”，而是当前局部斜率规则有效。
2. `stride=2` vs `stride=2+3`
   - 验证 `stride=3` 是否值得保留。
3. `dynstride` vs `阶段重采样/阶段重加权`
   - 区分收益到底来自时间压缩，还是来自高磨损阶段样本权重变化。

执行顺序：
1. 先做 `stride=2` vs `2+3`；
2. 再做 `random` vs `slope-guided`；
3. 最后做 `dynstride` vs `stage-resample/stage-weight-only`。

## 6.1 机制消融最新结论（已完成）
1. `stride=2` vs `2+3`
   - `stride=3` 没有实际贡献；
   - 当前有效收益全部来自 `stride=2`。
2. `random-S2` vs `slope-S2`
   - 当前不能证明 slope-guided 规则优于随机分配；
   - 数量匹配的 `random-S2` 三折平均反而略优。
3. `StageWeightOnly-S2`
   - 单纯提高高磨损阶段样本权重，不能复现 `dynstride` 的收益；
   - 三折平均甚至略差于主基线。
4. 因而当前最稳机制结论是：
   - **有效的是 `stride=2` 的时间压缩增强本身；**
   - **不是 `stride=3`；**
   - **也不是单纯高磨损样本重加权。**

## 6.2 下一步正式执行计划（已确定）
1. 先把 A2 正式收敛成最终版：
   - `A2-final = A2-Random-S2`
   - 只保留 `stride=2`
   - 不再把 `slope-guided` 作为主贡献
2. 正式确认实验只保留两组：
   - `Baseline-final = RMS7 + Feat_4`
   - `A2-final = RMS7 + Feat_4 + A2-Random-S2`
3. 本轮只回答一个问题：
   - A2 收敛后，是否能稳定提升当前最强特征基线，尤其是 hardest fold。
4. 输出要求：
   - 每轮单独 `results/<round_name>/`
   - 必须生成预测曲线
   - 统一报告 `fullcurve_raw MAE/RMSE`

## 7. 当前文件清单（核心）
1. 结果与计划：
   - `结果.md`
   - `实施计划2.md`
   - `process.md`
   - `2026.3.20沟通.txt`
2. A2 dynstride 关键代码：
   - `data_provider/data_loader.py`
   - `scripts/run_fold1_rms7_plus1_feat4_a2_dynstride_seed2026.sh`
   - `scripts/run_fold2_rms7_plus1_feat4_a2_dynstride_seed2026.sh`
   - `scripts/run_fold3_rms7_plus1_feat4_a2_dynstride_seed2026.sh`
3. A2 dynstride 可视化：
   - `feature_alignment_diagnosis/outputs/A2_dynstride_diagnostics_20260323_1743/`
4. StageWeightOnly 消融结果与曲线：
   - `results/20260323_A2_stageweightonly_s2/`


## A2之后的主线收敛（2026-03-24）
1. 后续不再表述为“继续找更强特征”，而是：
   - **检验在 `A2(S2-only)` 之后，是否仍存在与时间压缩增强正交的物理信息。**
2. 正式对照矩阵固定为：
   - `Baseline-final = RMS7 + Feat_4`
   - `A2-final = RMS7 + Feat_4 + A2(S2-only)`
   - `A2 + Feat_k = RMS7 + Feat_4 + A2(S2-only) + Feat_k`
3. 第一批候选优先级：频域 / 时频域 / 局部非平稳能量类；暂不优先慢变趋势型时域特征。


### 第一轮候选执行结果：`SPEC_CENTROID_CH1`
1. `A2 + SC1` 已完成三折训练。
2. `SC1` 在 `fold2/fold3` 有增益，但 hardest fold `c1,c4 -> c6` 明显恶化：
   - `A2-final`: `9.5255`
   - `A2 + SC1`: `15.7273`
3. 当前结论：
   - `SC1` 不是稳定的正交补充信息；
   - 不进入正式主线。

### 方案B（下一步）
1. 下一候选改为：`HF_ENERGY_RATIO_CH1`（通道1高频能量占比）。
2. 不直接起三折训练，先做轻量诊断：
   - run 内 wear 相关性；
   - 训练域间分离程度；
   - 与 `SC1` 并排比较。
3. 只有诊断结果优于 `SC1`，才进入正式三折训练。

### 方案B执行结果
1. `HF_ENERGY_RATIO_CH1` 轻量诊断通过。
2. 关键证据：
   - 平均 `|Spearman(wear)|`：`HF=0.9131`，`SC1=0.1467`
   - 平均训练域分离偏离 `|AUC-0.5|`：`HF=0.0116`，`SC1=0.3250`
3. 当前结论：
   - 下一轮正式三折候选改为 `HF_ENERGY_RATIO_CH1`；
   - `SC1` 停止推进。

### `HF_ENERGY_RATIO_CH1` 正式三折结果
1. `A2 + HF1`：
   - `9.8552 / 4.1748 / 3.6231`
   - 三折平均 `5.8844`
2. 相对 `A2-final`：
   - 平均略好；
   - 但 hardest fold 未继续改善。
3. 当前定位：
   - `HF1` 为保留候选；
   - 不直接升级为正式最优。
4. 下一步：
   - 继续同类第二候选 `HF_ENERGY_RATIO_CH2`；
   - 只改变通道，不改变高频能量占比定义。

### `HF_ENERGY_RATIO_CH2` 正式三折结果
1. `A2 + HF2`：
   - `11.6055 / 4.7559 / 3.9060`
   - 三折平均 `6.7558`
2. 相对 `HF1`：
   - hardest fold 更差；
   - fold2 更差；
   - 整体不如 `HF1`。
3. 当前定位：
   - `HF2` 不准入；
   - `HF1` 保留为候选；
   - 不再继续横向试更多 `HF ratio` 通道。
4. 下一步建议：
   - 改测 `SPECTRAL_ENTROPY_CH1`，
   - 从高频占比切到谱结构复杂度信息。

### `SPECTRAL_ENTROPY_CH1` 正式三折结果
1. `A2 + SE1`：
   - `9.0247 / 4.4520 / 3.7052`
   - 三折平均 `5.7273`
2. 相对 `A2-final`：
   - 平均继续改善；
   - hardest fold 也继续改善。
3. 当前定位：
   - `SE1` 是目前第一个同时满足“平均更优 + hardest fold 更优”的谱结构候选；
   - 可正式进入主线。
4. 当前最优主线更新为：
   - `RMS7 + Feat_4 + A2-Random-S2 + SPECTRAL_ENTROPY_CH1`

## 8. SE1 之后的下一步（已定方向，未执行）
1. 当前不再优先继续加普通单特征。
2. 当前锚点配置固定为：
   - `Current-best = RMS7 + Feat_4 + A2-Random-S2 + SPECTRAL_ENTROPY_CH1`
3. 下一步先做：
   - `fold1(c1,c4->c6)` 分段误差分析；
   - 检查误差是否主要集中在后期快速退化段。
4. 若确认后段是主问题，则后续方法优先转向：
   - 轻量训练域表示对齐（`CORAL / MMD`）；
   - 目标是继续降低 `c6` 的跨域误差。
5. 当前不优先：
   - 更多普通单特征；
   - 更复杂 `stride`；
   - `stagealign`；
   - `KNN` 检索增强；
   - 测试域自适应。
6. 新增固定口径：
   - 分段分析按真实 wear 进度三等分；
   - 同时记录对应真实 wear 区间（um）；
   - `underest_ratio = mean(pred < true)`。
7. `CORAL` 先只做 `fold1` 轻量验证：
   - 主标准：overall MAE 下降、late-stage MAE 下降、late-stage residual 更接近 `0`；
   - 附加标准：补跑三折后再检查 `fold2/fold3` 是否明显恶化。
8. `Asymmetric Loss` 只在以下条件成立时触发：
   - `late` 段误差最大；
   - `late` 段系统性低估最明显；
   - 这种低估显著强于 `early/mid`。

## 8.1 `CORAL` 单折验证结果（已完成）
1. 对象：
   - `Current-best + CORAL`
   - 只跑 `fold1(c1,c4->c6)`。
2. 结果：
   - overall `fullcurve_raw MAE`: `9.0247 -> 9.0258`
   - `late-stage MAE`: `27.0208 -> 27.0098`
   - `late-stage mean residual`: `-27.0208 -> -27.0098`
   - `late-stage underest_ratio`: `1.0000 -> 1.0000`
3. 结论：
   - `CORAL` 未通过主标准；
   - 当前剩余问题仍是 `late` 段系统性低估。
4. 下一步：
   - 进入最小 `MMD` 单折验证。
5. `AsymLoss` 首轮验证配置已锁定：
   - 主线特征包不动；
   - 固定 `wear_threshold_um = 150.0`；
   - `asym_alpha = 2.0`；
   - 只做 `fold1` 单折验证。
6. `AsymLoss` 两轮结果：
   - `Asym150`: `fold1 MAE = 9.3283`，`mid` 明显恶化，`late` 未改善；
   - `Asym196`: `fold1 MAE = 9.8416`，比 `Asym150` 更差；
   - `late-stage underest_ratio` 仍为 `1.0000`。
7. 当前收口：
   - `CORAL` 基本无效；
   - `MMD` 仅有极弱正信号；
   - `AsymLoss` 当前实现不通过，暂时冻结；
   - 正式主线仍为 `Current-best = RMS7 + Feat_4 + A2-Random-S2 + SPECTRAL_ENTROPY_CH1`。
8. 下一步主线已切换为 `KNN retrieval` 最小验证：
   - 不再继续扩大 `loss` 微调；
   - 先只做 `fold1(c1,c4->c6)`。
9. `KNN` 的物理定位：
   - 它只能对训练库做插值，不能外推出超过 `c1/c4` 历史最大磨损的值；
   - 因此目标不是替代回归头外推极值，而是在 `c6` 晚期提供更稳的经验兜底。
10. 第一轮 `KNN` 只比较三组：
   - `head-only`
   - `knn-only`
   - `head+knn blend`
11. 第一轮最小配置：
   - 表示层：`shared_repr`
   - 距离：`cosine`
   - `K = 5 / 10`
   - `beta = 0.3 / 0.5`
12. `KNN retrieval` fold1 最小验证结果：
   - `head-only = 9.0247`，与 `Current-best` 完全一致；
   - `knn-only@k5 = 25.2692`
   - `knn-only@k10 = 25.6555`
   - 最佳融合 `blend@k5_b03 = 13.4435`
13. 当前结论：
   - 这版 `KNN` 检索与融合均显著差于 `head-only`；
   - 当前 `shared_repr + cosine + raw label vote` 路线不通过。
14. 下一步切换为 `Retrieval V2`：
   - 检索对象改为未来增量 `delta = future_wear - current_last_wear`；
   - 融合方式改为 `pred_final = pred_head + beta * delta_knn`；
   - 第一轮继续只做 `fold1`，并优先只用高磨损训练窗口建库。
15. `Retrieval V2` 首轮结果：
   - 固定 `150um` 子库在 train split 中筛空，不作为有效结论；
   - 有效 `q80` 子库版本：阈值 `125.8911 um`，库大小 `66`
   - `head-only = 9.0247`
   - `delta-knn-only@k5 = 2.0357`
   - `delta-blend@k5_b03 = 9.1119`
16. 当前结论：
   - `delta retrieval` 本身有强信号；
   - 当前失败点主要在融合公式，而不是检索目标。
17. 下一步进入 `Retrieval V2.1`：
   - 只修正融合公式；
   - 新公式为：
     - `delta_head = pred_head - current_last_wear`
     - `pred_final = pred_head + beta * (delta_knn - delta_head)`
18. `Retrieval V2.1` 结果：
   - `head-only = 9.0247`
   - `delta-blend@k5_b03 = 6.2464`
   - `delta-blend@k5_b05 = 4.5552`
19. 当前结论：
   - `delta retrieval` 方向成立；
   - 关键改进来自残差式融合公式。
20. 三折 `Retrieval V2.1` 结果：
   - `fold1: MAE 9.0247 -> 4.5552, RMSE 15.4064 -> 7.1528`
   - `fold2: MAE 4.4520 -> 3.2214, RMSE 5.1674 -> 3.9766`
   - `fold3: MAE 3.7052 -> 3.4591, RMSE 4.5311 -> 4.0645`
21. 三折平均：
   - `Current-best MAE = 5.7273`
   - `Retrieval V2.1 MAE = 3.7452`
   - `Current-best RMSE = 8.3683`
   - `Retrieval V2.1 RMSE = 5.0646`
22. 当前最强方案：
   - `RMS7 + Feat_4 + A2-Random-S2 + SPECTRAL_ENTROPY_CH1 + Retrieval V2.1`
