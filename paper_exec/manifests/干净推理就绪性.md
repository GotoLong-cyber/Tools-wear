# Clean Inference Readiness

更新时间：2026-04-01

## 判断对象

本文件只判断：**是否已满足执行 clean retrieval inference 的前提**。

## 前提条件

1. retrieval-backbone clean 三折 checkpoint 全部存在
2. 三折 retrieval-backbone 日志中均出现最终 `fullcurve_raw` 指标
3. 训练协议为 `--disable_train_test_eval`
4. retrieval 超参数固定
5. retrieval library 仍为 train-only

## 当前判定

### 条件核查

| 条件 | 当前状态 | 结论 |
| --- | --- | --- |
| clean retrieval-backbone fold1 checkpoint | 已存在 | 满足 |
| clean retrieval-backbone fold2 checkpoint | 已存在 | 满足 |
| clean retrieval-backbone fold3 checkpoint | 已存在 | 满足 |
| fold1 最终 `fullcurve_raw` | 已出现 | 满足 |
| fold2 最终 `fullcurve_raw` | 已出现 | 满足 |
| fold3 最终 `fullcurve_raw` | 已出现 | 满足 |
| 训练期 test 监控移除 | 已通过协议补丁实现 | 满足 |
| fixed retrieval hyperparameters | 已在 formal 协议中固定 | 满足 |

## 结论

- **执行 clean retrieval inference 的前提：已满足**
- **执行 paper 级 clean evidence closure 的前提：未满足**

原因：

- baseline clean 三折未齐
- TMA clean 三折未齐
- clean retrieval inference 本身尚未真正执行

## 当前阻塞点

不是 retrieval-backbone，而是 baseline/TMA 训练尚未完整闭环。

## 下一步唯一动作

等待并补齐 baseline clean 与 TMA clean 的缺失折次，然后立刻执行 clean retrieval inference。
