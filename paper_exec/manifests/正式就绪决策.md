# Formal Ready Decision

更新时间：2026-04-01

## 第二轮结果对象（按优先级）

1. baseline clean 三折 checkpoint 是否完整
2. TMA clean 三折 checkpoint 是否完整
3. retrieval-backbone clean 三折 checkpoint 是否完整
4. clean retrieval inference 是否已基于 clean backbone 运行
5. `paper_exec` 的 figures / tables / csv / captions / snippets 是否已全部刷新
6. `论文资产看板.md` 中哪些资产可以从 blocked 改为 formal-ready，哪些仍必须保持 blocked

## 结果验收门槛表

| 对象 | 从 blocked 升级为 formal-ready 的必要条件 | 当前是否满足 |
| --- | --- | --- |
| baseline clean 单折结果 | 来自 clean protocol；训练期不含 test monitoring；存在该折 checkpoint；存在该折最终 `fullcurve_raw`；execution_log 与 manifest 可对齐 | 否 |
| baseline clean 三折结果 | 上述条件对 fold1/2/3 全部成立 | 否 |
| TMA clean 单折结果 | 来自 clean protocol；训练期不含 test monitoring；存在该折 checkpoint；存在该折最终 `fullcurve_raw`；execution_log 与 manifest 可对齐 | 部分满足 |
| TMA clean 三折结果 | 上述条件对 fold1/2/3 全部成立 | 否 |
| retrieval-backbone clean 单折结果 | 来自 clean protocol；训练期不含 test monitoring；存在该折 checkpoint；存在该折最终 `fullcurve_raw`；execution_log 与 manifest 可对齐 | 是 |
| retrieval-backbone clean 三折结果 | 上述条件对 fold1/2/3 全部成立 | 是 |
| clean retrieval inference 单折结果 | 必须由 clean retrieval-backbone checkpoint 驱动；固定 `k=5, beta=0.5, late_q=0.80`；train-only library；产出 inference 日志与结果文件；execution_log 记录完整 | 否 |
| clean retrieval inference 三折结果 | 上述条件对 fold1/2/3 全部成立 | 否 |
| `main_results_3fold.csv` | baseline/TMA/retrieval 三条 clean 结果齐全；CSV 已刷新；execution_log 记录刷新动作；不混入 blocked 证据 | 否 |
| `hardest_scenario_results.csv` | hardest fold 来源于 clean pipeline；CSV 已刷新；不混入 blocked 旧数值 | 否 |
| `retrieval_ablation.csv` | clean retrieval inference 三折已完成；CSV 已刷新；不混入 blocked 旧数值 | 否 |
| `stage_residual_compare.csv` | clean baseline/TMA/retrieval inference 三者齐全；CSV 已刷新；不混入 blocked 旧数值 | 否 |
| `retrieval_quality_before_after_a2.csv` | clean retrieval inference 已完成；比较两侧都来自 clean checkpoint | 否 |
| `tma_ablation.csv` | clean baseline 与 clean TMA 三折齐全；CSV 已刷新 | 否 |
| 图件 / 表格 / captions / snippets | 必须由 clean CSV/结果驱动重生成；dashboard 与 manifest 已同步更新 | 否 |

## 当前改判结论

### 可以改判为 formal-ready 的对象

- 无

说明：虽然 retrieval-backbone clean 三折已齐，但它只是 retrieval inference 的前置 backbone，不等同于最终论文主结果。

### 仍必须保持 blocked 的对象

- `paper_exec/csv/main_results_3fold.csv`
- `paper_exec/csv/hardest_scenario_results.csv`
- `paper_exec/csv/retrieval_ablation.csv`
- `paper_exec/csv/stage_residual_compare.csv`
- `paper_exec/csv/retrieval_quality_before_after_a2.csv`
- `paper_exec/csv/tma_ablation.csv`
- `paper_exec/captions/*`
- `paper_exec/tables/*`
- `paper_exec/figures/*`
- `paper_exec/结果片段_中文.md`
- `paper_exec/结果片段_英文.md`
- `paper_exec/论文资产看板.md`
- `paper_exec清单.md`

## 当前阻塞点

- baseline clean 三折未齐
- TMA clean 三折未齐
- clean retrieval inference 尚未运行
- `paper_exec` 尚未由 clean 结果刷新

## 下一步唯一动作

补齐 baseline clean 与 TMA clean 的缺失折次；在三折全部齐全后，立即执行 clean retrieval inference，然后统一刷新 `paper_exec` 全部资产并重新判定 formal-ready。
