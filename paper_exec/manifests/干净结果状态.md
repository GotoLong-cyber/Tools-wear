# Clean Result Status

更新时间：2026-04-01

## 唯一事实源

本文件只依据以下内容判定：

- `协议补丁说明.md`
- `干净运行计划.md`
- `clean_run_manifest.csv`
- `干净检查点索引.md`
- `执行日志.md`
- 当前 `results/20260401_*` 目录中的日志、状态文件、checkpoint 文件

## 第二轮必须检查的结果对象（按优先级）

1. baseline clean 三折 checkpoint 是否完整
2. TMA clean 三折 checkpoint 是否完整
3. retrieval-backbone clean 三折 checkpoint 是否完整
4. clean retrieval inference 是否已基于 clean backbone 运行
5. `paper_exec` 的 figures / tables / csv / captions / snippets 是否已全部刷新
6. `论文资产看板.md` 中哪些资产可以从 blocked 改为 formal-ready，哪些仍必须保持 blocked

## 逐折状态

状态定义仅允许：

- `complete`
- `missing`
- `invalid`

### 1. baseline clean

| fold | 状态 | 可检查证据 | 结论 |
| --- | --- | --- | --- |
| fold1 | complete | `fullcurve_raw` 指标已落盘，checkpoint 存在；旧 wrapper 仅误报 FAIL，不影响训练本体结果 | 可作为 clean 单折结果 |
| fold2 | missing | `20260401_BaselineClean_resume_gpu3` 补跑已启动，但当前尚无最终 `fullcurve_raw` 判定 | 未完成 |
| fold3 | missing | 无 fold3 clean 结果目录日志与 checkpoint | 未完成 |

### 2. TMA clean

| fold | 状态 | 可检查证据 | 结论 |
| --- | --- | --- | --- |
| fold1 | complete | `fullcurve_raw` 指标已落盘，checkpoint 存在 | 可作为 clean 单折结果 |
| fold2 | complete | `fullcurve_raw` 指标已落盘，checkpoint 存在 | 可作为 clean 单折结果 |
| fold3 | missing | 未生成 fold3 训练日志与 checkpoint | 未完成 |

### 3. retrieval-backbone clean

| fold | 状态 | 可检查证据 | 结论 |
| --- | --- | --- | --- |
| fold1 | complete | `fullcurve_raw` 指标已落盘，checkpoint 存在 | 可作为 clean 单折结果 |
| fold2 | complete | `fullcurve_raw` 指标已落盘，checkpoint 存在 | 可作为 clean 单折结果 |
| fold3 | complete | `fullcurve_raw` 指标已落盘，checkpoint 存在 | 可作为 clean 单折结果 |

## 当前阻塞点

- baseline clean 三折不完整
- TMA clean 三折不完整
- clean retrieval inference 尚未执行
- `paper_exec` 尚未由 clean 结果刷新

## 下一步唯一动作

先补齐 baseline clean 与 TMA clean 的缺失折次，并确认三折最终 `fullcurve_raw` 指标齐全；在此之前，任何 `paper_exec` 资产都不得改判为 formal-ready。
