# 实施计划2.md（重建版）

更新时间：2026-03-17

## 一、先纠正共识（非常重要）
1. `c1,c4 -> c6` 这种“训练在 c1/c4，测试在 c6”的设置，本身**不构成数据泄露**。
2. 是否泄露的判断标准不是“有没有用到 c6”，而是：
   - 当前测试域是否参与了任何拟合或选择决策。
3. 因此正确约束应为：**当前折的测试域封存**，不是“c6 永久封存”。

## 二、实验总方案：三折跨工况（LOCO）
固定三组实验，三组都要做：
1. Fold-1：训练 `c1,c4`，测试 `c6`
2. Fold-2：训练 `c1,c6`，测试 `c4`
3. Fold-3：训练 `c4,c6`，测试 `c1`

说明：每一折里，被测试的那一把刀是“当前测试域”，必须封存。

## 三、零泄露硬约束（按“当前折”生效）
对每一折，`test_run` 一律禁止参与以下过程：
1. 特征提取规则拟合（若提取过程含可学习参数）。
2. 特征选择与打分（树重要性、相关性排序、RFE 等）。
3. 标准化/归一化/缺失值填充参数拟合。
4. 超参数搜索、早停、模型选择、融合权重确定。
5. 任何使用标签的统计或监督信号构建。
6. 任何使用 `test_run` 无标签分布来决定阈值、排序、筛选规则或特征去留。

只允许两件事：
1. 模型冻结后，对 `test_run` 做一次最终盲测。
2. 盲测后做结果可视化与误差分析（不允许回流改模型）。

## 四、每一折的统一执行流程
1. 数据划分：
   - 训练域（两把刀）内部按 `8:2` 切分 train/val。
   - `test_run` 全程不参与切分和调参。
2. 特征工程（仅训练域）：
   - 原始数据 -> 候选特征提取。
   - 稳定性过滤（只在训练域及跨折训练域比较）。
   - 冗余过滤（高相关去重）。
   - 预测性排序（树/单变量回归/RFE，均只用训练域）。
3. 训练：
   - 保持现有 TimerXL 算法与损失形式不改。
   - 双 DataLoader 仅在训练域两把刀之间混合。
4. 选择模型：
   - 仅根据 val 指标确定模型与超参。
5. 最终评估：
   - 在 `test_run` 上一次性输出 window/fullcurve/fullcurve_raw 指标。

## 五、特征选择策略（从“普通 top-k”升级）
不再直接“全量候选 -> tree top-k”。改为两阶段：

### 阶段 A：稳定性过滤（训练域内部拟合 + 跨折训练域一致性）
1. 在当前折训练域内执行稳定性过滤，并汇总跨折训练域的一致性统计（不使用当前折 `test_run`）。
2. 删除跨训练域偏移过大的特征（按标准化均值偏移阈值）。
3. 删除与 wear 关系方向不一致、波动过大的特征（先在训练域内判定，再做跨折一致性复核）。
4. 删除高冗余特征（例如相关系数 > 0.98 只留一个）。
5. 产出两份结果：
   - 严格零泄露可训练特征集（仅由训练域决定）；
   - 跨折训练域一致性分析报告（用于解释与后续调参，不回流本折拟合）。

### 阶段 B：预测性选择（在 A 的剩余特征上，强调跨折/跨刀具稳定性）
1. 在每一折内先做预测性排序（树重要性/单变量回归/RFE），得到折内候选排名。
2. 不只看单折验证集表现，必须汇总跨折（`c1,c4->c6`、`c1,c6->c4`、`c4,c6->c1`）的一致性。
3. 特征入选标准改为“预测性 + 稳定性”双约束：
   - 折内有效（验证集表现不劣）；
   - 跨折/跨刀具排名稳定（波动小、方向一致）。
4. 首轮优先试 `k=6/8/10`，不再固定死 `k=16`。
5. 最终特征集由“跨折综合分”决定，而不是某一折最优分决定；当前折 `test_run` 不参与任何选择决策。

## 六、训练与评估协议（统一口径）
1. 模型：保持当前 TimerXL 结构与物理约束损失，不改算法。
2. 数据加载：双 DataLoader，训练域两把刀分别构窗后混合采样。
3. 指标：
   - 主报告：`window/fullcurve/fullcurve_raw` 的 MAE/MSE/RMSE。
   - 补充：跨折均值与标准差。
4. 可视化：每折输出窗口预测图、全曲线图、测试结果目录。
5. 训练轮次与早停：
   - `max_epochs=1000`；
   - 启用严格早停（仅基于 val），并恢复最佳权重做最终测试；
   - 早停策略全实验统一，不因折或特征候选单独调整。
6. GPU 强约束（2026-03-17 15:33 生效）：
   - 只要涉及实验训练/评估，一律使用 GPU；
   - 禁止 CPU fallback；
   - 实验脚本必须先通过 `nvidia-smi` 与 `torch.cuda.is_available()` 预检。

## 七、验收标准
1. 三折全部完成，且每折都有独立日志与图像产物。
2. 每折都能提供“零泄露检查记录”（见下一节）。
3. 汇总表包含：
   - 每折指标
   - 三折均值±方差
   - 特征数量与特征名单
4. 可明确回答：
   - 哪组特征在跨折最稳定。
   - 是否优于“原始 tree-k16”基线。

## 八、每折必填的零泄露检查清单
每次开跑前/后都记录以下 5 项：
1. 当前 `test_run` 是否参与任何拟合步骤：必须为“否”。
2. 特征选择是否仅在训练域完成：必须为“是”。
3. 标准化参数是否仅由训练域拟合：必须为“是”。
4. 模型选择/早停是否未参考 `test_run`：必须为“是”。
5. `test_run` 是否仅用于最终一次盲测：必须为“是”。

## 九、立即执行顺序（当前回合）
1. 先做 Fold-1：`c1,c4 -> c6`（按新策略重跑，严格零泄露）。
2. 你验收 Fold-1 后，再做 Fold-2：`c1,c6 -> c4`。
3. 最后做 Fold-3：`c4,c6 -> c1`。

## 十、阶段A执行记录（Fold-1，2026-03-16）
1. 执行范围：
   - 拟合域：`c1,c4`（训练/验证域）
   - 导出域：`c1,c4,c6`（`c6` 仅做特征导出，不参与拟合）
2. 输入数据：
   - `dataset/passlevel_tree_select/base_td28/{c1,c4,c6}_passlevel_td28.npz`
3. 产物目录：
   - `scripts/round_stageA_stability_20260316/results`
   - `scripts/round_stageA_stability_20260316/data`
4. 阶段A结果：
   - 总特征：`28`
   - 保留：`17`
   - 剔除：`11`
5. 剔除类型统计：
   - 高偏移（`shift_sigma > 2.0`）：`0`
   - 关系不稳（符号冲突/波动大）：`1`（`Feat_13`）
   - 高冗余（`|corr| >= 0.98`）：`10`
6. 保留特征（17）：
   - `Feat_22, Feat_4, Feat_24, Feat_16, Feat_18, Feat_20, Feat_7, Feat_10, Feat_9, Feat_5, Feat_8, Feat_3, Feat_25, Feat_1, Feat_28, Feat_26, Feat_21`
7. 导出矩阵形状：
   - `c1`: `(315, 17)`
   - `c4`: `(315, 17)`
   - `c6`: `(315, 17)`
8. 零泄露检查结论（本折）：
   - `test_run=c6` 未参与阶段A拟合与筛选，只做同列导出；符合本计划约束。

## 十一、阶段A重跑记录（Fold-1 rerun，2026-03-16）
1. 执行口径不变：
   - 拟合域：`c1,c4`
   - 导出域：`c1,c4,c6`（`c6` 仅导出）
2. 本次导出后缀：`passlevel_td28_stageA_fold1_rerun`
3. 重跑结果与首次一致：
   - 总特征 `28`，保留 `17`，剔除 `11`
   - 剔除构成：`high_shift=0`、`unstable_corr=1`（`Feat_13`）、`redundant=10`
4. 重跑导出矩阵形状：
   - `c1/c4/c6` 均为 `(315,17)`
5. 结论：阶段A筛选过程稳定，可进入阶段B。

## 十二、阶段B执行记录（Fold-1，2026-03-16）
1. 执行口径（严格 `Inductive`）：
   - 排序拟合域：`c1,c4`
   - 交叉验证分裂：`train=c4,val=c1` 与 `train=c1,val=c4`
   - `c6` 仅用于导出同列特征，不参与排序拟合。
2. 阶段B方法：
   - 单变量线性回归（按验证 MAE 排名）；
   - 树模型重要性（RandomForest）；
   - RFE 排名；
   - 跨分裂稳定性惩罚（rank std）；
   - 综合分：`0.5*rank_lin + 0.3*rank_tree + 0.2*rank_rfe + 0.3*stability_penalty`（越小越优）。
3. 输入特征数：`17`（阶段A输出）。
4. 输出候选集：
   - `k=6`：`Feat_4, Feat_22, Feat_7, Feat_18, Feat_10, Feat_3`
   - `k=8`：在 `k=6` 基础上增加 `Feat_20, Feat_8`
   - `k=10`：在 `k=8` 基础上增加 `Feat_24, Feat_16`
5. 导出数据形状：
   - `k=6`: `c1/c4/c6` 均为 `(315,6)`
   - `k=8`: `c1/c4/c6` 均为 `(315,8)`
   - `k=10`: `c1/c4/c6` 均为 `(315,10)`
6. 关键产物：
   - `results/feature_rank_stageB.csv`
   - `results/keep_features_stageB_k6.txt`
   - `results/keep_features_stageB_k8.txt`
   - `results/keep_features_stageB_k10.txt`
   - `results/stageB_summary.json`

## 十三、阶段B训练评估记录（Fold-1，2026-03-16）
1. 评估对象：阶段B输出的 `k=6/8/10`（均为 dual loader，`seed2026/e200/bt96`）。
2. 预训练加载检查：三组日志均为 `[Pretrain] matched=140 skipped=2`。
3. 最终测试结果（MSE/MAE）：
   - `k6`：window `926.03/19.19`，fullcurve `1004.59/20.01`，fullcurve_raw `1004.59/20.01`
   - `k8`：window `1042.49/20.19`，fullcurve `1120.82/21.12`，fullcurve_raw `1120.82/21.12`
   - `k10`：window `1123.05/21.21`，fullcurve `1209.73/21.99`，fullcurve_raw `1209.73/21.99`
4. 阶段B结论（Fold-1，单seed）：`k6` 最优。
5. 过程约束核查：
   - 阶段B选择仍为严格 `Inductive`（只用 `c1,c4` 决策特征）。
   - `c6` 仅参与最终盲测，不参与阶段A/B拟合与阈值决策。
6. 后续进入条件：
   - 若继续推进最终结论，需按本计划完成 Fold-2（`c1,c6->c4`）与 Fold-3（`c4,c6->c1`）同口径评估。

## 十四、下一步讨论版计划（2026-03-17，不执行）
当前共识：`RMS7 + Feat_4` 已验证“`+1` 增广”可行，下一步可以继续加特征，但必须从“盲目加维”改为“受控加维”。

### 14.1 目标
1. 目标不是把特征越堆越多，而是找到“对三折跨工况都稳”的最小增广集合。
2. 主比较对象固定为：
   - 基线A：`RMS7`
   - 基线B：`RMS7 + Feat_4`
3. 新方案必须在至少两折上优于基线B，且第三折不显著退化，才允许晋级。

### 14.2 候选特征来源（顶刊常见思路，先规划不执行）
在时域基础上，分三层候选池：
1. 形状类：`kurtosis/skewness/crest_factor/impulse_factor/shape_factor`。
2. 频域类：`band_energy_ratio/spectral_centroid/main_peak_freq`。
3. 时频类：`WPT节点能量占比/STFT高频能量占比`。

说明：
1. 候选池先建“公式与提取定义”，不做一次性大拼接。
2. 每个候选优先先做“单通道 +1”验证，再考虑多通道扩展。

### 14.3 评估与准入门槛（硬约束）
1. 严格零泄露：每折 `test_run` 不参与任何拟合、排序、阈值确定。
2. 双向训练域验证（折内）：
   - 方向A：`train=c1,val=c4`
   - 方向B：`train=c4,val=c1`
3. 准入门槛（单个候选）：
   - 两个方向 `MAE` 都不劣于基线B；
   - 且至少一个方向有显著下降（建议阈值 `>=2%`）。
4. 三折盲测门槛（进入结果汇总前）：
   - `hardest fold` 不劣于基线B；
   - 三折平均 `MAE` 改善；
   - 至少两折优于基线B；
   - 最差一折不超过基线B的 `+5%` 退化。

### 14.4 执行顺序（下一轮按此推进）
1. 第一步：定义候选特征公式表（只建表，不训练）。
2. 第二步：先做“单通道 +1”。
3. 第三步：通过门槛的候选，再试“多通道稀疏 +1”。
4. 第四步：若某一候选在多通道下仍稳定，再试“对称 +1”。
5. 第五步：通过门槛后再做 `+2` 组合测试（最多到 `+3`，避免维度膨胀）。
6. 第六步：按三折结果定稿“最终轻量特征集”。

### 14.5 当前结论（讨论版）
1. 不再默认“对称 +1 优先”，当前证据更支持“单通道受控增广优先”。
2. 不把 `e1000` 作为默认标准，后续筛选阶段必须统一轮次或统一早停策略。
3. 今后比较候选特征时，必须单独盯住 `hardest fold`；当前默认按 `c1,c4 -> c6` 作为 hardest fold 管理。
4. 可以继续加特征，但必须采用“逐个候选、逐级准入”的策略。
5. 当前优先级建议：先试形状类，再试频域，再试时频。

## 十五、表征对齐诊断流程（每折，计划阶段）
1. 目标域定义必须分离：
   - 折内验证目标域：用于模型选择（如 `train=c1,val=c4` 时的 `c4`）。
   - 真正盲测目标域：当前折 `test_run`（如 `c6`），仅用于最终盲测与事后诊断。
2. 标准化规则：
   - 仅用训练域拟合标准化器；
   - 用同一标准化器变换 train/val/test 特征。
3. 磨损分段着色规则：
   - 分段阈值仅由训练域 wear 分位数拟合（如 33%/66%）；
   - 固定阈值后再应用到 val/test。
4. 可视化主辅图规则：
   - `PCA` 作为固定主图（复现稳定）；
   - `UMAP` 作为辅助图（可选）。
5. 每折输出图像：
   - 图A：PCA 按域着色；
   - 图B：PCA 按磨损分段着色；
   - 辅助图：UMAP（按域/按磨损分段）；
   - 关键特征分布图（KDE/箱线图）。
6. 定量对齐指标：
   - `MMD`；
   - `CORAL distance`；
   - 域分类 `AUC`（单特征）；
   - 域分类 `AUC`（整体特征集）。
7. 零泄露边界：
   - `test_run` 仅用于事后诊断；
   - 不回流参与本折特征选择、阈值设定、模型调整。

## 十六、阶段十四启动记录（2026-03-17 15:13，Step 1 完成）
1. 已启动阶段十四，并先完成“候选定义与队列固化”（不执行训练）。
2. 已新增候选公式表：
   - `feature_alignment_diagnosis/configs/stage14_candidate_formula_table.md`
3. 已新增单通道 `+1` 可机读队列：
   - `feature_alignment_diagnosis/configs/stage14_single_channel_plus1_queue.yaml`
4. Step 1 产出内容：
   - 明确 `td28` 的 `Feat_1~Feat_28` 公式映射；
   - 明确 `RMS7` 与 `RMS7+Feat_4` 的基线特征集合；
   - 固化第一批单通道 `+1` 候选顺序（`Feat_8/10/12/16/18/20/22/24/28`）。
5. 当前状态：
   - 阶段十四 Step 1 已完成；
   - 待验收后进入 Step 2（按队列执行单通道 `+1` 实验）。

## 十七、阶段十四 Step 2 启动记录（2026-03-17 15:16）
1. 已启动 `hardest fold`（`c1,c4 -> c6`）的单通道 `+1` 队列实验。
2. 新增执行脚本：
   - `feature_alignment_diagnosis/scripts/run_step2_fold1_single_plus1_queue_seed2026.sh`
3. 训练协议（本轮）：
   - `max_epochs=1000`；
   - `patience=100`（严格早停，基于验证集）；
   - GPU 单卡（`--gpu 0`）；
   - 默认先跑 `MAX_CANDIDATES=1` 作为 Step 2 起跑。
4. 后台任务信息：
   - `PID=1828350`
   - 主日志：`results/master_step2_fold1_single_plus1_seed2026.log`
5. 当前状态：
   - Step 2 已进入运行态；
   - 待首个候选完成后记录结果并决定是否扩展到更多候选。
6. 运行态确认（2026-03-17 15:20）：
   - 持久会话方式启动成功（会话ID：`77625`）；
   - 首个候选：`Feat_8`；
   - 训练日志已写入：`results/longrun_PHM_c1c4_to_c6_rms7_plus_feat4_plus_feat_8_dual_seed2026_e1000_bt96_gpu1_step2.log`；
   - 日志已出现 `Epoch 1` 记录，确认任务在执行中。
7. GPU硬约束下重启确认（2026-03-17 15:38）：
   - 采用主机GPU模式会话启动（会话ID：`84542`）；
   - GPU预检通过：`nvidia-smi` 可见 3 卡、`cuda_available=True`；
   - 当前日志已推进到 `Epoch 37`，继续运行中。

## 十八、阶段十四 Step 2 首候选完成（2026-03-17 16:21）
1. 首个候选 `+Feat_8` 已完成（fold1：`c1,c4 -> c6`）：
   - 日志：`results/longrun_PHM_c1c4_to_c6_rms7_plus_feat4_plus_feat_8_dual_seed2026_e1000_bt96_gpu1_step2.log`
   - 结果：`window MAE=14.6179`、`fullcurve MAE=15.3493`
   - 结论：相对 `RMS7+Feat_4` 基线未提升，暂不准入。
2. 已并行进入第十五节“表征对齐诊断”执行（仅诊断，不回流训练）：
   - 输出目录：`feature_alignment_diagnosis/outputs/fold1_alignment_step2_20260317_1621/`
   - 发现：`+Feat_8` 未改善 train/test 表征对齐（MMD/CORAL 均略升）。
3. 下一动作：
   - 继续执行 Step2 队列的下一个单通道 `+1` 候选，沿同一口径复现并诊断。

## 十九、阶段十五可视化补充执行（2026-03-17 16:28）
1. 已按讨论补充“磨损连续着色”PCA诊断图，增强“域偏移 vs 磨损阶段差异”判读能力。
2. 本次新增图型（两组特征均已生成）：
   - `pca_by_wear_continuous.png`
   - `pca_by_wear_continuous_domain_panels.png`
3. 执行边界：
   - 仅补图，不新增训练；
   - 不改变第十四节候选筛选流程；
   - 严格保持零泄露与训练域拟合标准化约束。
